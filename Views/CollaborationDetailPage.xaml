<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:BrainWave.APP.ViewModels"
             xmlns:converters="clr-namespace:BrainWave.APP.Converters"
             x:Class="BrainWave.APP.Views.CollaborationDetailPage"
             Title="Collaboration Details"
             BackgroundColor="{AppThemeBinding Light=#F8F9FA, Dark=#1A1A1A}">
    
    <ContentPage.Resources>
        <converters:DebugConverter x:Key="DebugConverter" />
    </ContentPage.Resources>

    <ScrollView>
        <VerticalStackLayout Padding="16" Spacing="16">
            
            <!-- Header -->
            <Frame BackgroundColor="{AppThemeBinding Light=#4A90E2, Dark=#2C5AA0}" 
                   CornerRadius="12" 
                   Padding="20" 
                   HasShadow="True">
                <VerticalStackLayout Spacing="8">
                    <Label Text="🤝 Collaboration Details" 
                           FontSize="20" 
                           FontAttributes="Bold" 
                           TextColor="White" 
                           HorizontalOptions="Center" />
                    <Label Text="Manage your collaboration settings" 
                           FontSize="14" 
                           TextColor="White" 
                           Opacity="0.9" 
                           HorizontalOptions="Center" />
                </VerticalStackLayout>
            </Frame>

            <!-- Collaboration Information -->
            <Frame BackgroundColor="{AppThemeBinding Light=#FFFFFF, Dark=#2D2D2D}" 
                   CornerRadius="12" 
                   Padding="20" 
                   HasShadow="True">
                <VerticalStackLayout Spacing="16">
                    
                    <!-- Collaboration Title -->
                    <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="10">
                        <Label Grid.Column="0" 
                               Text="Title:" 
                               FontSize="16" 
                               FontAttributes="Bold" 
                               TextColor="{AppThemeBinding Light=#333333, Dark=#FFFFFF}" 
                               VerticalOptions="Center" />
                        <Label Grid.Column="1" 
                               Text="{Binding Collaboration.Name, Converter={StaticResource DebugConverter}, FallbackValue='No title set'}" 
                               FontSize="16" 
                               TextColor="{AppThemeBinding Light=#666666, Dark=#CCCCCC}" 
                               VerticalOptions="Center"
                               BackgroundColor="{AppThemeBinding Light=#F8F9FA, Dark=#404040}"
                               Padding="8,4"
                               Margin="0,2" />
                        <Button Grid.Column="2" 
                                Text="Edit" 
                                BackgroundColor="{AppThemeBinding Light=#FF9800, Dark=#F57C00}" 
                                TextColor="White" 
                                CornerRadius="8" 
                                WidthRequest="50" 
                                HeightRequest="30" 
                                FontSize="12"
                                Clicked="EditTitle_Clicked" />
                    </Grid>

                    <!-- Collaboration Description -->
                    <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="10">
                        <Label Grid.Column="0" 
                               Text="Description:" 
                               FontSize="16" 
                               FontAttributes="Bold" 
                               TextColor="{AppThemeBinding Light=#333333, Dark=#FFFFFF}" 
                               VerticalOptions="Center" />
                        <Label Grid.Column="1" 
                               Text="{Binding Collaboration.Description, Converter={StaticResource DebugConverter}, FallbackValue='No description set'}" 
                               FontSize="16" 
                               TextColor="{AppThemeBinding Light=#666666, Dark=#CCCCCC}" 
                               VerticalOptions="Center"
                               BackgroundColor="{AppThemeBinding Light=#F8F9FA, Dark=#404040}"
                               Padding="8,4"
                               Margin="0,2" />
                        <Button Grid.Column="2" 
                                Text="Edit" 
                                BackgroundColor="{AppThemeBinding Light=#FF9800, Dark=#F57C00}" 
                                TextColor="White" 
                                CornerRadius="8" 
                                WidthRequest="50" 
                                HeightRequest="30" 
                                FontSize="12"
                                Clicked="EditDescription_Clicked" />
                    </Grid>

                    <!-- Collaboration Role -->
                    <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="10">
                        <Label Grid.Column="0" 
                               Text="Your Role:" 
                               FontSize="16" 
                               FontAttributes="Bold" 
                               TextColor="{AppThemeBinding Light=#333333, Dark=#FFFFFF}" 
                               VerticalOptions="Center" />
                        <Label Grid.Column="1" 
                               Text="{Binding Collaboration.Role, Converter={StaticResource DebugConverter}, FallbackValue='No role set'}" 
                               FontSize="16" 
                               TextColor="{AppThemeBinding Light=#666666, Dark=#CCCCCC}" 
                               VerticalOptions="Center"
                               BackgroundColor="{AppThemeBinding Light=#F8F9FA, Dark=#404040}"
                               Padding="8,4"
                               Margin="0,2" />
                        <Button Grid.Column="2" 
                                Text="Edit" 
                                BackgroundColor="{AppThemeBinding Light=#FF9800, Dark=#F57C00}" 
                                TextColor="White" 
                                CornerRadius="8" 
                                WidthRequest="50" 
                                HeightRequest="30" 
                                FontSize="12"
                                Clicked="EditRole_Clicked" />
                    </Grid>

                    <!-- Related Task -->
                    <Frame BackgroundColor="{AppThemeBinding Light=#E8F4FD, Dark=#1A3A5C}" 
                           CornerRadius="8" 
                           Padding="12" 
                           HasShadow="False">
                        <VerticalStackLayout Spacing="8">
                            <Label Text="Related Task" 
                                   FontSize="14" 
                                   FontAttributes="Bold" 
                                   TextColor="{AppThemeBinding Light=#2C5AA0, Dark=#90CAF9}" />
                            <Label Text="{Binding TaskInfo}" 
                                   FontSize="12" 
                                   TextColor="{AppThemeBinding Light=#666666, Dark=#CCCCCC}" />
                        </VerticalStackLayout>
                    </Frame>

                    <!-- Token Information -->
                    <Frame BackgroundColor="{AppThemeBinding Light=#F3E5F5, Dark=#4A148C}" 
                           CornerRadius="8" 
                           Padding="12" 
                           HasShadow="False">
                        <VerticalStackLayout Spacing="8">
                            <Label Text="Collaboration Token" 
                                   FontSize="14" 
                                   FontAttributes="Bold" 
                                   TextColor="{AppThemeBinding Light=#7B1FA2, Dark=#BA68C8}" />
                            <Label Text="{Binding Collaboration.Token}" 
                                   FontSize="12" 
                                   TextColor="{AppThemeBinding Light=#666666, Dark=#CCCCCC}"
                                   FontFamily="Courier" />
                            <Button Text="Copy Token" 
                                    BackgroundColor="{AppThemeBinding Light=#9C27B0, Dark=#BA68C8}" 
                                    TextColor="White" 
                                    CornerRadius="6" 
                                    FontSize="12" 
                                    Padding="8,4"
                                    Clicked="CopyToken_Clicked" />
                        </VerticalStackLayout>
                    </Frame>

                </VerticalStackLayout>
            </Frame>


            <!-- Action Buttons -->
            <Frame BackgroundColor="{AppThemeBinding Light=#FFFFFF, Dark=#2D2D2D}" 
                   CornerRadius="12" 
                   Padding="16" 
                   HasShadow="True">
                <VerticalStackLayout Spacing="12">
                    
                    <!-- Management Actions -->
                    <Label Text="⚡ Management Actions" 
                           FontSize="16" 
                           FontAttributes="Bold" 
                           TextColor="{AppThemeBinding Light=#333333, Dark=#FFFFFF}" />
                    
                    <Grid ColumnDefinitions="*,*" ColumnSpacing="8">
                        <Button Grid.Column="0"
                                Text="💾 Save Changes" 
                                BackgroundColor="{AppThemeBinding Light=#4CAF50, Dark=#388E3C}" 
                                TextColor="White" 
                                CornerRadius="8" 
                                FontSize="12" 
                                FontAttributes="Bold" 
                                HeightRequest="40"
                                Clicked="SaveChanges_Clicked" />
                        
                        <Button Grid.Column="1"
                                Text="🚪 Leave" 
                                BackgroundColor="{AppThemeBinding Light=#FF9800, Dark=#F57C00}" 
                                TextColor="White" 
                                CornerRadius="8" 
                                FontSize="12" 
                                FontAttributes="Bold" 
                                HeightRequest="40"
                                Clicked="LeaveCollaboration_Clicked" />
                    </Grid>
                    
                    <!-- Delete Button -->
                    <Button Text="🗑️ Delete Collaboration" 
                            BackgroundColor="{AppThemeBinding Light=#F44336, Dark=#D32F2F}" 
                            TextColor="White" 
                            CornerRadius="8" 
                            FontSize="12" 
                            FontAttributes="Bold" 
                            HeightRequest="40"
                            Clicked="DeleteCollaboration_Clicked" />
                </VerticalStackLayout>
            </Frame>

            <!-- Back Button -->
            <Button Text="← Back to Collaborations" 
                    BackgroundColor="{AppThemeBinding Light=#6C757D, Dark=#495057}" 
                    TextColor="White" 
                    CornerRadius="12" 
                    FontSize="14" 
                    FontAttributes="Bold" 
                    HeightRequest="50"
                    Command="{Binding BackCommand}" />

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>