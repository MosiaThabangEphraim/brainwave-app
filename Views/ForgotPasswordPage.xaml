<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:BrainWave.APP.ViewModels"
             x:Class="BrainWave.APP.Views.ForgotPasswordPage"
             Title="Forgot Password"
             BackgroundColor="{AppThemeBinding Light=#F8F9FA, Dark=#1A1A1A}">

    <ContentPage.BindingContext>
        <vm:ForgotPasswordViewModel />
    </ContentPage.BindingContext>

    <ScrollView>
        <VerticalStackLayout Padding="40" Spacing="30" VerticalOptions="Center">
            
            <!-- Header -->
            <Frame BackgroundColor="{AppThemeBinding Light=#4A90E2, Dark=#2C5AA0}" 
                   CornerRadius="20" 
                   Padding="30" 
                   Margin="0,0,0,40"
                   HorizontalOptions="Center">
                <VerticalStackLayout>
                    <Label Text="🔐" 
                           FontSize="48" 
                           HorizontalOptions="Center" />
                    <Label Text="Forgot Password" 
                           FontSize="28" 
                           FontAttributes="Bold" 
                           TextColor="White" 
                           HorizontalOptions="Center" />
                    <Label Text="Reset your password" 
                           FontSize="14" 
                           TextColor="White" 
                           Opacity="0.9" 
                           HorizontalOptions="Center" />
                </VerticalStackLayout>
            </Frame>

            <!-- Reset Form -->
            <Frame BackgroundColor="{AppThemeBinding Light=#FFFFFF, Dark=#2D2D2D}" 
                   CornerRadius="16" 
                   Padding="30" 
                   HasShadow="True">
                <VerticalStackLayout Spacing="20">
                    <Label Text="Enter your email address *"
                           FontSize="24"
                           FontAttributes="Bold" 
                           TextColor="{AppThemeBinding Light=#333333, Dark=#FFFFFF}" 
                           HorizontalOptions="Center" />
                    
                    <Label Text="We'll send you a password reset token if your email exists in our system" 
                           FontSize="14" 
                           TextColor="{AppThemeBinding Light=#666666, Dark=#CCCCCC}" 
                           HorizontalOptions="Center" />

                    <Frame BackgroundColor="{AppThemeBinding Light=#F5F5F5, Dark=#404040}" 
                           CornerRadius="12" 
                           Padding="16" 
                           HasShadow="False">
                        <Entry Placeholder="Email Address (Required)" 
                               Text="{Binding Email}" 
                               Keyboard="Email"
                               BackgroundColor="Transparent" 
                               TextColor="{AppThemeBinding Light=#333333, Dark=#FFFFFF}"
                               PlaceholderColor="{AppThemeBinding Light=#999999, Dark=#AAAAAA}" />
                    </Frame>

                    <Button Text="Send Password Token" 
                            Command="{Binding SendTokenCommand}" 
                            BackgroundColor="{AppThemeBinding Light=#4A90E2, Dark=#2C5AA0}" 
                            TextColor="White" 
                            CornerRadius="12" 
                            FontSize="16" 
                            FontAttributes="Bold" 
                            Padding="20" 
                            IsEnabled="{Binding IsFormValid}" />

                    <Label Text="{Binding ErrorMessage}"
                           TextColor="Red"
                           HorizontalOptions="Center" 
                           FontSize="14" />

                    <Button Text="Back to Login" 
                            Command="{Binding BackToLoginCommand}"
                            BackgroundColor="Transparent"
                            TextColor="{AppThemeBinding Light=#4A90E2, Dark=#90CAF9}" 
                            FontSize="14" />
                </VerticalStackLayout>
            </Frame>

            <!-- Footer -->
            <Frame BackgroundColor="{AppThemeBinding Light=#E8F5E8, Dark=#1B5E20}" 
                   CornerRadius="12" 
                   Padding="16" 
                   Margin="0,20,0,0">
                <VerticalStackLayout>
                    <Label Text="BrainWave 2025" 
                           FontSize="14" 
                           FontAttributes="Bold" 
                           TextColor="{AppThemeBinding Light=#2E7D32, Dark=#A5D6A7}" 
                           HorizontalOptions="Center" />
                </VerticalStackLayout>
            </Frame>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
