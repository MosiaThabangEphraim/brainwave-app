<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:BrainWave.APP.ViewModels"
             x:Class="BrainWave.APP.Views.LoginPage"
             Title="Login"
             BackgroundColor="{AppThemeBinding Light=#F8F9FA, Dark=#1A1A1A}">

    <ContentPage.BindingContext>
        <vm:LoginViewModel />
    </ContentPage.BindingContext>

    <ScrollView>
        <VerticalStackLayout Padding="40" Spacing="30" VerticalOptions="Center">
            
            <!-- Logo/Header -->
            <Frame BackgroundColor="{AppThemeBinding Light=#4A90E2, Dark=#2C5AA0}" 
                   CornerRadius="20" 
                   Padding="30" 
                   Margin="0,0,0,40"
                   HorizontalOptions="Center">
                <VerticalStackLayout>
                    <Label Text="🧠" 
                           FontSize="48" 
                           HorizontalOptions="Center" />
                    <Label Text="BrainWave" 
                           FontSize="28" 
                           FontAttributes="Bold" 
                           TextColor="White" 
                           HorizontalOptions="Center" />
                    <Label Text="Productivity Made Simple" 
                           FontSize="14" 
                           TextColor="White" 
                           Opacity="0.9" 
                           HorizontalOptions="Center" />
                </VerticalStackLayout>
            </Frame>

            <!-- Login Form -->
            <Frame BackgroundColor="{AppThemeBinding Light=#FFFFFF, Dark=#2D2D2D}" 
                   CornerRadius="16" 
                   Padding="30" 
                   HasShadow="True">
                <VerticalStackLayout Spacing="20">
            <Label Text="Welcome Back"
                   FontSize="24"
                           FontAttributes="Bold" 
                           TextColor="{AppThemeBinding Light=#333333, Dark=#FFFFFF}" 
                           HorizontalOptions="Center" />
                    
                    <Label Text="Sign in to continue your productivity journey" 
                           FontSize="14" 
                           TextColor="{AppThemeBinding Light=#666666, Dark=#CCCCCC}" 
                           HorizontalOptions="Center" />

                    <!-- Login Type Selection -->
                    <Label Text="Login As:" 
                           FontSize="16" 
                           FontAttributes="Bold" 
                           TextColor="{AppThemeBinding Light=#333333, Dark=#FFFFFF}" />
                    
                    <Grid ColumnDefinitions="*,*" ColumnSpacing="10">
                        <Frame Grid.Column="0" 
                               BackgroundColor="{Binding IsUserLogin, Converter={StaticResource BoolToColorConverter}}" 
                               CornerRadius="12" 
                               Padding="15" 
                               HasShadow="False">
                            <Frame.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding SelectUserLoginCommand}" />
                            </Frame.GestureRecognizers>
                            <VerticalStackLayout HorizontalOptions="Center" Spacing="5">
                                <Label Text="👤" FontSize="24" HorizontalOptions="Center" />
                                <Label Text="User" 
                                       FontSize="14" 
                                       FontAttributes="Bold" 
                                       TextColor="{Binding IsUserLogin, Converter={StaticResource BoolToTextColorConverter}}" 
                                       HorizontalOptions="Center" />
                            </VerticalStackLayout>
                        </Frame>
                        
                        <Frame Grid.Column="1" 
                               BackgroundColor="{Binding IsAdminLogin, Converter={StaticResource BoolToColorConverter}}" 
                               CornerRadius="12" 
                               Padding="15" 
                               HasShadow="False">
                            <Frame.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding SelectAdminLoginCommand}" />
                            </Frame.GestureRecognizers>
                            <VerticalStackLayout HorizontalOptions="Center" Spacing="5">
                                <Label Text="🛡️" FontSize="24" HorizontalOptions="Center" />
                                <Label Text="Admin" 
                                       FontSize="14" 
                                       FontAttributes="Bold" 
                                       TextColor="{Binding IsAdminLogin, Converter={StaticResource BoolToTextColorConverter}}" 
                                       HorizontalOptions="Center" />
                            </VerticalStackLayout>
                        </Frame>
                    </Grid>

                    <Frame BackgroundColor="{AppThemeBinding Light=#F5F5F5, Dark=#404040}" 
                           CornerRadius="12" 
                           Padding="16" 
                           HasShadow="False">
                        <Entry Placeholder="{Binding LoginPlaceholder}" 
                               Text="{Binding Email}" 
                               Keyboard="{Binding LoginKeyboard}"
                               BackgroundColor="Transparent" 
                               TextColor="{AppThemeBinding Light=#333333, Dark=#FFFFFF}"
                               PlaceholderColor="{AppThemeBinding Light=#999999, Dark=#AAAAAA}" />
                    </Frame>

                    <Frame BackgroundColor="{AppThemeBinding Light=#F5F5F5, Dark=#404040}" 
                           CornerRadius="12" 
                           Padding="16" 
                           HasShadow="False">
                        <Grid ColumnDefinitions="*,Auto">
                            <Entry Grid.Column="0"
                                   Placeholder="Password"
                                   IsPassword="{Binding IsPasswordVisible, Converter={StaticResource InvertedBoolConverter}}"
                                   Text="{Binding Password}" 
                                   BackgroundColor="Transparent" 
                                   TextColor="{AppThemeBinding Light=#333333, Dark=#FFFFFF}"
                                   PlaceholderColor="{AppThemeBinding Light=#999999, Dark=#AAAAAA}" />
                            <Button Grid.Column="1"
                                    Text="{Binding PasswordVisibilityIcon}"
                                    Command="{Binding TogglePasswordVisibilityCommand}"
                                    BackgroundColor="Transparent"
                                    TextColor="{AppThemeBinding Light=#666666, Dark=#CCCCCC}"
                                    FontSize="16"
                                    Padding="8,0"
                                    BorderWidth="0" />
                        </Grid>
                    </Frame>

                    <Button Text="Sign In" 
                            Command="{Binding LoginCommand}" 
                            BackgroundColor="{AppThemeBinding Light=#4A90E2, Dark=#2C5AA0}" 
                            TextColor="White" 
                            CornerRadius="12" 
                            FontSize="16" 
                            FontAttributes="Bold" 
                            Padding="20" />

                    <!-- Forgot Password Button -->
                    <Button Text="Forgot Password?" 
                            Command="{Binding ForgotPasswordCommand}"
                            BackgroundColor="Transparent"
                            TextColor="{AppThemeBinding Light=#666666, Dark=#CCCCCC}" 
                            FontSize="14" 
                            HorizontalOptions="End" />

            <Label Text="{Binding ErrorMessage}"
                   TextColor="Red"
                           HorizontalOptions="Center" 
                           FontSize="14" />

                    <Button Text="Don't have an account? Sign Up" 
                    Command="{Binding GoToRegisterCommand}"
                    BackgroundColor="Transparent"
                            TextColor="{AppThemeBinding Light=#4A90E2, Dark=#90CAF9}" 
                            FontSize="14" />

                    <!-- Exit Button -->
                    <Button Text="🚪 Exit App" 
                            Command="{Binding ExitCommand}"
                            BackgroundColor="{AppThemeBinding Light=#F44336, Dark=#D32F2F}" 
                            TextColor="White" 
                            CornerRadius="8" 
                            FontSize="14" 
                            FontAttributes="Bold"
                            Padding="16,8"
                            HorizontalOptions="Center" />
                </VerticalStackLayout>
            </Frame>

            <!-- Footer -->
            <Frame BackgroundColor="{AppThemeBinding Light=#E8F5E8, Dark=#1B5E20}" 
                   CornerRadius="12" 
                   Padding="16" 
                   Margin="0,20,0,0">
                <VerticalStackLayout>
                    <Label Text="BrainWave 2025" 
                           FontSize="14" 
                           FontAttributes="Bold" 
                           TextColor="{AppThemeBinding Light=#2E7D32, Dark=#A5D6A7}" 
                           HorizontalOptions="Center" />
                </VerticalStackLayout>
            </Frame>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
